import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((them) =>({
    fab:{
        position: 'fixed',
        bottom: them.spacing(12),
        left: them.spacing(67),
    },

    fab1:{
        position: 'fixed',
        bottom: them.spacing(2),
        left: them.spacing(67),
    },
}));